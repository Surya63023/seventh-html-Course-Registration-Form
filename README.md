# Course Registration Form - Mini Project

A **comprehensive HTML form project** demonstrating full mastery of HTML form elements, input validation, and form handling. This project is part of learning web development fundamentals, focusing on creating user-friendly, functional forms.

---

## 🌟 Features

- **Personal Details Section**:
  - First Name & Last Name (text inputs with validation)
  - Mobile Number (tel input)
  - Email Address (email input)
  - Password & Confirm Password fields (min length validation)
  - Date of Birth (date picker)
  - Gender selection (radio buttons)
  - Country selection (dropdown)
  - Profile Picture upload (file input)
- **Course Details Section**:
  - Course selection (dropdown)
  - Department selection (radio buttons)
  - Delivery mode selection (online/offline)
  - Feedback section (textarea)
- **Form Controls**:
  - Submit button → redirects to `response.html`
  - Reset button → clears all input fields
- **Validation**:
  - Uses HTML attributes (`required`, `minlength`, `maxlength`, `type`) for front-end validation
- **User Experience Enhancements**:
  - Clear placeholders and labels
  - Organized using `fieldset`, `legend`, and tables

---

## 🚀 How to Run

1. Clone the repository:

```bash
git clone https://github.com/yourusername/CourseRegistrationForm.git


